import React from 'react';
import {
    Animated,
    Image,
    StyleSheet,
    Text,
    useAnimatedValue,
    View,
  } from 'react-native';

export default function Media(){

    return(
        // -----------------------
        <Animated.View>

        </Animated.View>
    )
}